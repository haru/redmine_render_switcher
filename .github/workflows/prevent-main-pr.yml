name: Main Branch PR Policy

on:
  pull_request_target:
    # edited fires when the base branch is changed, so a PR opened against
    # develop cannot be retargeted to main to dodge the policy.
    types: [opened, reopened, edited]
    branches:
      - main

permissions:
  pull-requests: write
  issues: write

jobs:
  prevent-main-pr:
    runs-on: ubuntu-latest
    # Allow PRs from release branches. Only act on open PRs: an edit to a PR this
    # job already closed must not comment on it again.
    if: ${{ github.event.pull_request.state == 'open' && !startsWith(github.head_ref, 'release/') }}
    steps:
      - name: Comment and close PR
        uses: actions/github-script@v9
        with:
          script: |
            const message = "## ⚠️ Direct pull requests to main branch are not allowed. Please create a pull request to the develop branch instead.";

            // Add comment
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });

            // Close the PR
            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              state: 'closed'
            });

            console.log('PR has been commented and closed automatically');

  label-release-pr:
    runs-on: ubuntu-latest
    # Add ignore-for-release label to PRs from release branches
    if: ${{ startsWith(github.head_ref, 'release/') }}
    steps:
      - name: Add ignore-for-release label
        uses: actions/github-script@v9
        with:
          script: |
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: ['ignore-for-release']
            });
